import '../styles/global.css';

import { ChallengesProvider } from '../contexts/ChallangeContex';

function MyApp({ Component, pageProps }) {
  return (
      <Component {...pageProps} />
  
  )
}

export default MyApp